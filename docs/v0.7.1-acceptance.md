# 0.7.1 驗收與發布狀態

日期：2026-09-13。追蹤：[Issue 131](https://github.com/tedliou/td-cli/issues/131)。

## 範圍

交付 #129 的 Constant CHOP Sequence 去重，並加入 canonical 0.7.0 Agent 的精確離線升級白名單。0.7.0 除 manifest 外，五份內嵌來源的其餘雜湊與 0.6.0 完全一致，因此保留一份共同內容，明確覆寫 0.7.0 manifest hash。未改 Request lifecycle、transport 或自動重送策略。

## 公開來源與 locked 驗收

從公開 v0.7.0 release 實際下載 td-agent-component-v0.7.0-td2025.32050.zip 並比對 SHA256SUMS。ZIP SHA256：`7eda271f4faa4b587e6d72c9fee8e4d23a053ae6562cce38f78aa63add58d0aa`。

在 TD 2025.32050 新建 disposable project，載入已驗證 0.7.0 artifact，保留 sentinel Constant CHOP 值42。未開啟、修改或關閉使用者作品。

正式介面：`uv run td-agent upgrade-project <scratch.toe> --artifact <new-agent.tox> --manifest <new-agent.tox.manifest.json> --tools-dir <locked TD bin> --expected-sha256 <source hash> --timeout 90`，外層總期限100秒。

- [upgrade.json](evidence/v071/upgrade.json)：status upgraded，原始備份SHA與來源相同。
- [same-version.json](evidence/v071/same-version.json)：status unchanged，SHA保持不變。
- [cold.json](evidence/v071/cold.json)：PID5280冷啟後 Agent0.7.1、sentinel42、Sequence只列出name/value各一次。
- [build.json](evidence/v071/build.json)：candidate來源revision及artifact精確SHA。

所有自有測試PID均已退出。第一次cold把「Socket inactive」錯當條件而失敗；canonical Agent正常初始化應啟用Socket。主代理協調獨占時段後重測正常啟動成功，未引入抑制啟動或替代transport的產品變更。

## 自動驗收與簡化

擴充現有 migration fixture，公開介面先於 unsupported version 失敗，再通過0.7.0來源；保留非Agent圖形、備份、同版no-op、破壞輸入拒絕等既有驗收。精確白名單重用相同0.6.0來源hash，不增加推測版本支援。

完整local gate：517 tests、ruff check/format、mypy src、uv lock --check、Agent source inspection、git diff --check全部通過。獨立review核對remote ZIP、五份內嵌hash、artifact/backup/升級toe SHA與文件一致，無實質finding。最終exact-main staging identity紀錄於Issue131。Candidate驗收並不代表發布或安裝完成。

## 發布 gate

本輪只讀稽核：實際ruleset只有General Protection，main/develop只禁止刪除及non-fast-forward；classic protections不存在。release environment要求tedliou真人批准；既有v0.7.0 immutable=true。

`docs/release-operations.md` 要求發版前的CI/approval/local-evidence/promotion/tag保護目前尚不完整。代理未改管理政策，保留此發布gate；可先完成source、PR、promotion與exact-main staging，再由管理者處理缺口及環境批准。不得以本文件取代或放寬正式發布要求。
